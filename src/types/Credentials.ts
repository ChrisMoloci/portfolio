/**
 * Represents a Credentials submitted to the  API
 *
 * @author Christian Moloci
 *
 * @type email email of the user
 * @type password password of the user
 */
export type Credentials = {
    email: string,
    password: string,
}